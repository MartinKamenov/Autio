export type TranslationObject = {
    navbar: {
        brands: string
    },
    home: {
        search_header: string
    },
    toggle: string
};

export type LanguageState = {
    languageKey: 'en' | 'bg'
};
